name: TLU Login Account 2
run-name: Check Login Account 2

on:
  push:
    branches: [main, develop, "feature/**", "fix/**"]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  login-account-2:
    name: Check Account 2
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: temurin
          cache: maven

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Check Login Account 2
        run: mvn test -Dtest=com.nguyenanhvu.logintest.TluLoginTest#testLoginAccount2
        env:
          CI: true
